# supabase/

Placeholder. `supabase/` is the player plane (build plan §3.1 row F):
Supabase Postgres + RLS + Auth + Storage + Edge Functions (Deno), the single
source of truth for users, devices, evidence, purchase evidence, plays,
achievements, entitlements, offers, connector tokens and the audit log.

**Migrations and Edge Functions arrive in P3** (build plan §10, §15) — money
and personal data do not go live before then, and P3's pre-build gate
(second admin named, break-glass kit made and dry-run, external security
reviewer engaged, production catalog keyset generated) has to be met first.

Nothing is built here in P0 beyond this placeholder.
