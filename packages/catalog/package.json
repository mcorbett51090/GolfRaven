{
  "name": "@golfraven/catalog",
  "private": true,
  "version": "0.0.0",
  "description": "GolfRaven catalog schema SSOT: the §4.1 Zod schema (Facility/Course/Trail/RosterVersion/OfferTerms/etc.) plus the ID ledger, minted by tools/catalog's verify-contract into contract/catalog.schema.json (build plan §3.1 row A, §4.1). Includes vendored tzdb-backward-links.json and region-codes.json for pinned tz/region validation. P1a scope: AchievementDef and loadCatalog() are not yet implemented (part B / a later P1 step).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "tz-lookup": "6.1.25",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "@types/tz-lookup": "6.1.2",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
