# @golfraven/partners

Placeholder. `apps/partners` is the staff/operator/admin portal PWA (build
plan §3.1 row H): the course-QR screens (O5), the special-marker hand-over
screen and per-shop stock ledger (O9/O10), and, from P6, the read-only
sponsor view (O11). It is a static SPA served from a separate origin
(`partners.golfraven.<tld>`) with a strict CSP.

Real work starts at **P5**. Nothing is built here in P0 beyond this
placeholder, so the workspace has a slot for it.
