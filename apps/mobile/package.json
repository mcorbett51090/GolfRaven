{
  "name": "@golfraven/mobile",
  "private": true,
  "version": "0.0.0",
  "description": "GolfRaven mobile app (Expo/React Native, build plan §3.1 row G, §7). P0 scope: only the Android Health Connect reader for check X1 (build plan §10 P0).",
  "main": "index.ts",
  "scripts": {
    "build": "echo \"@golfraven/mobile: no EAS/expo export build in the P0 skeleton (real builds start P4, build plan §10). typecheck is the meaningful gate here.\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "expo": "57.0.24",
    "react": "19.2.3",
    "react-native": "0.86.3",
    "react-native-health-connect": "4.1.3"
  },
  "devDependencies": {
    "@types/react": "19.2.18",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
