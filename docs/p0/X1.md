# X1 (=K4a) — Health real-device test

## Check (verbatim, plan §10 P0)

> **Health real-device test** (**2 elapsed days**: a real round is ~4 h, then an Android pass). One real round
> from 3 sources (Garmin watch + Connect Mobile; Apple Watch Workout; one phone golf app) on iOS, then an
> equivalent Android pass. Inspect `workoutActivityType`, `HKWorkoutRoute`, `HKSource.bundleIdentifier`, and the
> Health Connect exercise type / route / `dataOrigin`. **Harness (G-P0-06):** iOS = Apple Health's export
> (`export.xml` + its per-workout route GPX files) `[unverified — training knowledge]`, no code needed; Android =
> a minimal Health Connect reader built in the P0 skeleton (0.5 pw, reused by P4).

## Pass bar (verbatim)

> ≥ 2 of 3 sources write golf workouts **with routes** on ≥ 1 OS. **The memo records a verdict per source**
> (A2-14), so a Garmin-source fail is visible even when X1 passes.

## Kill consequence (verbatim)

> Health lane ships **date-only** (§7.3 lane 1); store copy drops "GPS match"; result feeds K4.

## Round windows

**Superseded by decision 0005: round windows are LABELS, not a filter.** A window logged here marks
the workouts inside it (with 60 minutes of slack either side) `testRound: true` in the tools' output.
It no longer excludes anything — every golf workout on the device counts, whenever it was played —
and `x1-ios-export`/`x1-verdict` no longer refuse to run when this section is blank. See decision 0005
for the full change and why (Matt's historical Garmin Approach S62 rounds).

_(blank — Matt may still log, for EACH test round, its start and end time in UTC, purely so those
workouts are tagged `testRound: true` in the output. One bullet per round, format: a bullet starting
with the round's UTC start timestamp, the word "to", then its UTC end timestamp — both in strict
`YYYY-MM-DDThh:mm:ssZ` form.)_

## Recorded export

_(blank until Matt logs each OS's date, BEFORE that OS's export is read — decision 0005 "Integrity
rules": "The first export read is the recorded one ... The same rule applies separately to each OS."
Format `YYYY-MM-DD`. `x1-ios-export`/`x1-verdict` refuse to produce a RECORDED result for an OS whose
line here is still blank; `--informational` runs anyway, producing an output marked `recorded: false`
with a loud banner, never the recorded P0 result. A later export can still be read informationally —
it never replaces the recorded result once one exists for that OS.)_

**Bound to one specific export (post-d0de4b8 Opus-gate correction).** The tools also check that the
export's own date (Apple's `ExportDate` for iOS; the Android reader's `generatedAt`) matches the date
logged here, and — on the first recorded run — append that export's SHA-256 to the SAME line as
`sha256:<hex>` (e.g. `- iOS: 2026-09-20 sha256:abcd...`). Every later recorded run for that OS must
hash to the same value, or it's refused: a recorded result is bound to one specific export file, not
just one calendar date. `--informational` is never checked against, or bound to, any of this.

- iOS:
- Android:

## METHOD

1. Play one real round of golf carrying: a Garmin golf watch running Garmin Connect Mobile; an Apple Watch
   running a Workout (Golf activity type where offered); and **18Birdies** as the phone golf app (Hole19 as
   the pre-round fallback if 18Birdies cannot be installed or offers no Health write setting — decision 0001,
   Addendum D, R6; both are on the plan's no-public-API app list, §7.3 row "—").
2. After the round, on the iPhone: **Settings → Health → profile icon → Export All Health Data**, producing
   `export.zip` (containing `export.xml` and a `workout-routes/` folder of GPX files per workout)
   `[unverified — training knowledge]`.
3. Inspect `export.xml` for each of the 3 sources' `<Workout>` elements: record `workoutActivityType`,
   `HKSource.bundleIdentifier`, and whether a matching GPX route file exists in `workout-routes/` (⇒
   `HKWorkoutRoute` present).
4. Run the equivalent round (or a second round) on Android with Health Connect enabled, using the P0-skeleton
   Health Connect reader (`apps/mobile` — built and unit-tested as of 2026-09-23, but **not device-tested**;
   see `apps/mobile/README.md` "Not device-tested" — a 0.5 pw prerequisite, reused by P4)
   to read the exercise type, route, and `dataOrigin` for each source that also has an Android app.
5. Record a **verdict per source** (Garmin, Apple Watch, phone app) × per OS, not just an overall pass/fail
   (A2-14) — a Garmin-source fail must stay visible even if the other two sources pass.
6. **CONSENT_REQUIRED rule (decision 0001, Addendum D, R6):** on Android, a session that Health Connect
   reports as `CONSENT_REQUIRED` counts as "route present" **only if** a follow-up `requestExerciseRoute` for
   that session's record id returns at least one point. Otherwise it counts as "not present". This is decided
   now, before the round, not after seeing which way a real result falls.
7. **The phone golf app is 18Birdies** (decision 0001, Addendum D, R6), fixed before the round. If 18Birdies
   cannot be installed, or offers no Apple Health / Health Connect write setting, **Hole19 replaces it**, and
   the reason is logged in this memo's Log below before the round. Any other app tried is supplementary data,
   never the source verdict.
8. **Every golf workout on the device counts, not just ones inside a logged round window (decision 0005).**
   A logged "Round windows" entry only tags a matching workout `testRound: true` in the tools' output; it does
   not gate whether the workout counts. Historical rounds on Matt's Garmin Approach S62 count the same as a
   newly-played one — their route must still be present, exactly as before.
9. **Log the "Recorded export" date for an OS BEFORE reading that OS's export (decision 0005).** `x1-ios-export`
   and `x1-verdict` both take `--os ios|android` and refuse to produce the RECORDED result for an OS whose date
   is still blank; pass `--informational` to run anyway (the output is then marked `recorded: false` with a
   loud banner — useful for a dry run, never the P0 verdict). The first export actually read for an OS, with
   its date logged first, is that OS's recorded one; a later export is informational only. `x1-ios-export`/
   `x1-verdict` also bind the run to that specific export (its own date compared against the logged date, and
   its SHA-256 bound on the first recorded run) — see the "Recorded export" section above.
10. **A recorded X1 verdict is decided per OS, from that OS's own data alone (post-d0de4b8 Opus-gate
    correction).** `x1-verdict --os ios` only counts as "recorded" — and only feeds the overall X1 pass/kill —
    when the `--ios` input is itself the JSON a recorded `x1-ios-export --os ios` run produced (not
    `--informational`, not a mismatched OS). The other OS's data, if passed alongside it, is shown for
    cross-reference but never folds into the recorded result. Run `x1-verdict` once per OS as each round's
    export is read; the overall X1 pass is "pass if either OS's own recorded verdict passes" (Addendum F's
    ≥ 2-of-3-on-one-OS bar, applied within that OS's data only).

## OWNER

**Matt-only physical step.** Requires a real ~4 h round of golf with borrowed hardware (see
`docs/owner/x1-k4b-device-protocol.md`) and cannot be run by an agent. The Health Connect reader script itself
(step 4's harness) is agent-buildable, but the round and the export are not.

## STATUS

OWNER — needs borrowed devices + real rounds.

## MEASURED VALUE

_(blank — not yet run)_

## VERDICT

_(blank: pass | kill | adjust)_

The results table (per source × OS, including the `CONSENT_REQUIRED` + follow-up-read column added by
decision 0001, Addendum D, R6) is filled in in `docs/owner/x1-k4b-device-protocol.md` §4, then copied here
into MEASURED VALUE once the round is run.

## Log

| Date | Entry |
|---|---|
| 2026-09-23 | Memo created from `docs/golf-trails/02-build-plan.md` §10 P0 and `docs/decisions/0001-owner-decisions-and-p0-thresholds.md`. No round has been played yet. |
| 2026-09-23 | Decision 0001, Addendum D (R6) recorded: CONSENT_REQUIRED counts as route-present only via a follow-up `requestExerciseRoute` read; phone app is 18Birdies, Hole19 as pre-round fallback. |
| 2026-09-23 | Decision 0001, Addendum F recorded (round-3 gate B-6/B-7): "≥ 1 OS" means ≥ 2 of 3 sources passing on the SAME operating system — sources passing on different OSes do not combine. A "Round windows" field was added above; `x1-ios-export`/`x1-verdict` now refuse to run until it is logged. |
| 2026-09-24 | Matt owns a **Garmin Approach S62** and has historical rounds on it. Those rounds **do not count** toward the X1 verdict: only rounds inside a logged round window count (Addendum F). His historical Apple Health or Health Connect export can still dry-run `x1-ios-export` against real data before the recorded round. **Superseded the same day — see the next row: decision 0005 reverses this "do not count" claim.** |
| 2026-09-24 | **Decision 0005 recorded, reversing the row above:** historical rounds on devices DO count toward the X1 verdict, whenever they were played. Round windows are now labels only (`testRound: true`/`false`), never a filter — nothing is excluded by date, and the tools no longer refuse to run when no window is logged. In its place, decision 0005 adds the "Recorded export" section and rule above: the first export read per OS, dated there before it's read, is that OS's recorded result; `--informational` runs any other read without it standing as the P0 verdict. Every counted workout is now listed with its date/source, and each source's newest counted workout date is shown, per `docs/decisions/0005-historical-rounds-count-for-x1.md`. |
| 2026-09-24 | **Opus-gate correction (commit d0de4b8 failed review).** The first decision-0005 implementation let a recorded `x1-verdict --os ios` run silently combine the OTHER (unrecorded/wrong-OS) input's data into the overall verdict — an `--os ios` run backed by an unrecorded `ios` JSON could still report an overall PASS purely from `android`'s raw, unstamped sessions. Fixed: the recorded verdict is now decided **per OS, from that OS's own stamped-`recorded` data alone** (METHOD step 10); the other OS's data is informational-only and never counted. The run is also now bound to one specific export — its own date matched against the logged date, and its SHA-256 bound on the first recorded run, refusing a later run against a different export (METHOD step 9, "Recorded export" section above). `--since` is refused on a recorded `x1-ios-export` run (no recency limit, decision 0005). The "newest counted workout date" now only considers workouts with a route (the verdict-bearing ones); a route-less workout's date is tracked separately. |
