# K2 — Player signal (organic-only)

## Check (verbatim, plan §10 P0)

> **Player signal (decoupled from operators, FM-06), organic-only (O23 DECIDED 2026-09-23: $0 paid promotion).**
> A two-trail landing page ("track & complete the X Trail, get the finisher's marker") on the **P0 domain**
> (`golfraven.<tld>`), live by day 3, promoted **organically**: golf communities and forums, the owner's own
> networks and social accounts, golf-trail hashtags and groups, and cross-links from SWC where its audience
> fits; operator channels are a bonus, not a dependency. Signups are double-opt-in through the custom SMTP.
> **Adjusted method:** the thresholds are unchanged (O7); the **day-14 read becomes advisory**, because organic
> reach ramps more slowly than paid reach and a low day-14 count is weak evidence either way; **the 6-week N =
> 300 stays the gate**. N is **not** lowered: a smaller bar would be easier to pass on noise, and the gate
> protects the costliest phase (P4.2).

## Pass bar (verbatim)

> **Early read (advisory):** ≥ 100 confirmed signups by day 14. **Full gate (verdict ≈ wk 7):** ≥ N = 300 in
> 6 wk (O7, fixed before any count is read).

## Kill consequence (verbatim)

> Early read < 100 → **advisory only**: the messaging and channel mix change that week and the count is
> logged; P1 is not held. Full miss → **P4.2 is held for an owner decision**, with two pre-written options:
> (a) take the upgrade path (≈ $1,000 of the ≈ $2,500, O23/O24) for **one** paid 2-week re-test at the same N,
> **during which P1–P4.1 proceed** (A2-15); or (b) accept the miss and replan. A second miss after a paid
> re-test is a replan, not another test.

## METHOD

1. Build and publish a two-trail landing page on `golfraven.<tld>` (the P0 domain — see
   `docs/owner/accounts-and-domain-checklist.md`), live by day 3, naming two of the pilot trails and the
   finisher's-marker hook.
2. Wire signups to double-opt-in through the custom SMTP (Postmark/SES with SPF/DKIM/DMARC — see the same
   checklist).
3. Promote **organically, $0 spend** (O23 DECIDED): golf communities/forums, Matt's own networks and social
   accounts, golf-trail hashtags/groups, and cross-links from `southern-wine-country` where its audience fits.
   Operator channels are a bonus, never a dependency.
4. Count **confirmed** (post-double-opt-in) signups at day 14 (advisory) and at 6 weeks (the gate, N = 300).
5. If day-14 < 100: log it as advisory, adjust messaging/channel mix, do **not** hold P1.
6. At 6 weeks, if N < 300: escalate to Matt for the pre-written owner decision (paid re-test vs. replan) —
   never auto-select an option.

## OWNER

**Matt-only for the promotion itself** — organic promotion through Matt's own social accounts, community
memberships, and personal network is not something an agent can do on his behalf. The landing page build and
the signup-count read are agent-runnable once the page and SMTP exist.

## STATUS

OWNER — landing page is **built** (`apps/landing`), but **not deployed** (blocked on domain, SMTP, and
the signup backend — see `apps/landing/README.md` "How signups work"); needs landing-page promotion
through the owner's own networks/social accounts and community outreach; an agent cannot post as Matt.

## MEASURED VALUE

_(blank — landing page not yet live and no signup backend exists yet, so no signups counted; K2's clock
has not started)_

## VERDICT

_(blank: pass | kill | adjust)_

## Log

| Date | Entry |
|---|---|
| 2026-09-23 | Memo created. No landing page exists yet; no signups have been counted. |
