# X6 — GolfNow ToS + partner-API docs

## Check (verbatim, plan §10 P0)

> Read the GolfNow ToS **and** partner-API docs on an open network: golfer-delegated scope? reservation read?
> who takes payment? (SP3)

## Pass bar (verbatim)

> Full text obtained **and** no clause forbids linking to public facility pages.

## Kill consequence (verbatim)

> A clause forbids deep links, **or** the text cannot be obtained by day 5 → T0 is course-native only until
> counsel L4 clears it.

## METHOD

**Exact documents to obtain** (from `docs/golf-trails/research/golfnow-integration.md`):

| Document | URL |
|---|---|
| GolfNow Terms of Use | `https://www.golfnow.com/support/about-us/terms` |
| GolfNow Affiliate & Partner API home | `https://affiliate.gnsvc.com/` |
| GolfNow Affiliate & Partner API "Getting Started" | `https://affiliate.gnsvc.com/getting-started` |
| GolfNow Business Partnership page | `https://www.golfnow.com/business-partnership` |

The research file flags that none of these were directly fetchable in that session either — everything on
file is from search-result snippets, and the ToS's specific clauses on scraping/automated access were "not
retrievable." X6 exists precisely to get the primary text.

**The three questions to answer from the fetched text (SP3, verbatim from the check):**

1. **Golfer-delegated scope?** — does the partner API expose a scope that lets a golfer authorize *us* to read
   their own GolfNow booking history, or is API access facility-side only?
2. **Reservation read?** — can an approved partner read confirmed-booking objects (course, date, time, player)
   through the API, which would function as a proof-of-play signal (§6.1 T1 row)?
3. **Who takes payment?** — does GolfNow (or the course, through GolfNow's checkout) take payment on a booking
   made through the partner flow, which matters for the "no scraping / no display of GolfNow prices" guardrail
   already assumed at T0 (§6.3)?

**Pass condition also requires:** scanning the ToS for any clause that forbids deep-linking to public facility
pages (the T0 mechanism, §6.1) or automated access generally (the no-scraping guardrail, §6.3).

## OWNER

Agent-runnable desk check — **once network access to `golfnow.com` and `affiliate.gnsvc.com` is unblocked.**

## STATUS

BLOCKED — network policy (`golfnow.com` confirmed proxy `403` this session; `affiliate.gnsvc.com` was not
separately probed this session but is the same integration surface — verify independently before assuming it
is open). Unblock in the environment's Network access settings (see `docs/owner/network-unblock.md`), then an
agent runs it. **Day-5 deadline is load-bearing** — the kill consequence fires if the text is not obtained by
then, independent of the pass/fail content.

## MEASURED VALUE

_(blank — not yet run)_

## VERDICT

_(blank: pass | kill | adjust)_

## Log

| Date | Entry |
|---|---|
| 2026-09-23 | Memo created. Confirmed this session: CONNECT to `golfnow.com` returns proxy 403; `WebFetch` also blocked. `affiliate.gnsvc.com` not separately tested this session. No fetch attempted or fabricated. |
