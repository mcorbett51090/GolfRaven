# X2 — Pilot-slate roster/rules direct-fetch

## Check (verbatim, plan §10 P0)

> Direct-fetch the pilot slate's official rosters and rules (TN, VI, RTJ — **O4 DECIDED 2026-09-23**; OK as
> reserve), plus 3 trail rules pages to settle `completionUnit` (SP7) and each slate trail's season window (for
> event-driven go-live, §9.6).

## Pass bar (verbatim)

> ≥ 2 slate trails confirmed, with roster + unit.

## Kill consequence (verbatim)

> Swap in the reserve: OK first, then Hammock Coast / Kauai.

## METHOD

Fetch each official site directly (not a search snippet — `research/canada-trails.md` flags that every prior
`WebFetch` for these hosts in this session returned `EGRESS_BLOCKED`, so a snippet is not sufficient evidence
per the P1 pre-build gate: "Pilot rosters confirmed by the operator or a direct fetch — never by a snippet.").

**Exact URLs to fetch** (from `docs/golf-trails/research/*`):

| Trail | Primary URL | Notes |
|---|---|---|
| Tennessee Golf Trail | `https://www.tnstateparks.com/golf` | Research host; the environment's blocked-host list also names `tngolftrail.net` and `tn.gov` — try all three, the canonical one is not yet confirmed |
| Vancouver Island Golf Trail | `https://golfvancouverisland.ca/` and `https://golfvancouverisland.ca/about-us/golf-trail-pass/` | Operator = "Golf Vancouver Island" cooperative; the Trail Pass terms page is `https://golfvancouverisland.ca/wp-content/uploads/2024/10/2024-GVI-Trail-Pass-Terms-and-Restrictions.pdf` (2024 edition — check for a current-season update) |
| Robert Trent Jones Golf Trail | `https://www.rtjgolf.com/` | Booking engine is separately at `https://rtjmembers.cps.golf/` (Club Prophet) — not needed for X2, relevant to X4/§6.1 |

**Parsing rule** — for each trail, from the fetched pages, extract and record:

1. **Roster** — the full list of member courses/facilities (name + site), checked against the counts already
   in `research/us-trails.md` / `research/canada-trails.md` (TN: 9 courses; VI: 11–13, conflicting; RTJ: 26
   courses / 11 facilities).
2. **`completionUnit`** (SP7) — does the trail's own published rule count a *course*, a *facility* (pro shop),
   or a *signature hole* (as with Great Okanagan) as the unit? Cite the exact rules-page text.
3. **Season window** — is the trail playable/tracked year-round (VI is reported "Canada's only year-round golf
   trail") or does it have a defined season? This feeds the event-driven go-live rule in §9.6.

A trail counts as "confirmed" only when roster + unit + season window are all sourced from the direct fetch,
not from a research-file snippet.

**Tooling (decision 0001 Addendum G, implemented literally).** `tools/p0`'s `x2-fetch` performs the direct
fetch above and stores evidence per Addendum G's "X2 'confirmed from a direct fetch'": raw bytes, final URL,
HTTP status, `fetchedAt` (UTC), a SHA-256 of the bytes, and an extracted-text file (HTML → text with inline
elements joined as a browser copies them and whitespace collapsed; a PDF's text is derived from its stored bytes
with the pinned `unpdf@1.8.1`). `x2-verdict` re-derives the text from the raw bytes and recomputes the SHA-256 at
verdict time, so a stored text copy is never trusted. Evidence counts for a trail only when its final URL's host
equals the configured host, or differs only by a leading `www.`; any other redirect is excluded and reported. It also prints a DRAFT
candidate-name list per trail (headings/link text) — a hint for whoever writes the confirmation below, never
itself a confirmation. `x2-verdict` then implements Addendum G's confirmation rule literally against a
human-written confirmation file (quotes + evidence SHAs): a trail is confirmed only when its roster,
`completionUnit` and season window are all backed by quotes appearing verbatim (after whitespace collapsing) in
the cited evidence, and every roster name appears in it. See `tools/p0/README.md` §§3-4 for exact invocations.

## OWNER

Agent-runnable desk check — **once network access to the listed hosts is unblocked.**

## STATUS

RUN — 2026-09-24. Network unblocked (see `docs/owner/network-unblock.md`). `x2-fetch`/`x2-verdict` ran
successfully (build: `pnpm --config.engine-strict=false --filter @golfraven/p0-tools build`). Node's built-in
`fetch` does not read `HTTPS_PROXY` by default in this environment (confirmed: without it, requests bypassed
the agent proxy entirely and hit a separate sandbox-level host allowlist returning its own
"Host not in allowlist" 403 — a different block from the site's own WAF); every tool invocation below used
`NODE_USE_ENV_PROXY=1` to route through the agent proxy, matching what plain `curl` already did.

## MEASURED VALUE

**0 of 3 slate trails confirmed** (bar: ≥ 2). Full per-trail detail in
`/tmp/golfraven-p0/x2-verdict-result.md` / `.json` (evidence: `/tmp/golfraven-p0/x2-evidence-final/`,
manifest + raw + text, SHA-256 per file).

- **TN — indeterminate: blocked by the site.** All three configured hosts fetched with the tool's own polite,
  bot-identifying User-Agent (`x2-fetch`'s `buildX2UserAgent()`, not a browser UA — deliberately never spoofed
  to get past a WAF):
  - `https://www.tnstateparks.com/golf` → **HTTP 403** (AWS ELB, `server: awselb/2.0`, generic "403 Forbidden"
    body — not our proxy's denial page/`x-deny-reason` header; confirmed by re-testing the same URL through
    plain `curl` with the same non-browser UA, still 403).
  - `https://www.tnstateparks.com/activities/golfing` (the real content page, found via web search once
    `/golf` 403'd) → **HTTP 403**, same WAF signature.
  - `https://tngolftrail.net/` → **HTTP 403** at `x2-fetch`'s hands; independently, `curl` shows it actually
    302-redirects to `https://tnstateparks.com/golf`, which is the same blocked page — so this host adds no
    new route around the block.
  - `https://www.tn.gov/` → **HTTP 200**, fetched cleanly (evidence sha `7270893e3f01…`), but it is Tennessee's
    general state portal — its extracted text (`draftCandidateNames`) is state-government news/nav only, with
    no golf-trail roster, unit or season content. A guessed `.tn.gov` state-parks path 404'd. `tn.gov`
    therefore does not supply an alternate route to the trail's own rules either.
  - As a spot-check (not part of the pre-registered URL list, so not evidence toward confirmation), a
    browser-UA `curl` to `/golf` got a 301→301 redirect chain into `tnstateparks.com/activities/golfing` and
    a 200 — confirming the block is genuinely a bot/UA-based WAF rule on the site, not our proxy — but that
    redirect chain also downgrades to `http:` mid-chain, which `x2-fetch`'s own gate N6 rejects, and using a
    spoofed browser UA to get around a site's own bot-blocking is not something this run does for the tool's
    real evidence-gathering fetch. **Per the task's instruction, this is recorded as TN indeterminate: blocked
    by the site — not a confirmed roster and not a guess.**
- **VI — fetched successfully (HTTP 200 both HTML pages), but genuinely has no confirmable content.**
  `golfvancouverisland.ca/` and its `/about-us/golf-trail-pass/` page are a client-side-rendered SPA (Inertia/
  Vue "Elemental" framework — `<div id="app" data-page="…JSON…">`, no server-rendered body text): the tool's
  extracted text is 69 and 80 bytes respectively — just the `<title>`. No roster, `completionUnit` or season
  text exists in the static HTML for a direct-fetch tool to quote (checked several more VI pages by hand —
  `/courses/`, `/about-us/`, `/about-us/media-room/fast-facts/` — all the same thin-shell pattern). The
  2024 Trail Pass Terms PDF at the one URL X2.md lists now returns **HTTP 404** (file removed/renamed on
  WordPress; no current filename found by search or by guessing 2025/2026-dated paths). VI is not blocked —
  it is unconfirmable from what the site actually serves without executing its JavaScript, which this tool
  correctly never does.
- **RTJ — roster confirmed at the fact level; trail still unconfirmed (missing 2 of 3 facts).**
  `rtjgolf.com/` and `rtjgolf.com/trail/` fetched cleanly (HTTP 200). `/trail/`'s "TRAIL SITES" section names
  all 11 sites with course/hole counts, e.g. `"Cambrian Ridge Greenville, AL 3 Courses; 27 Holes"` — every one
  of the 11 quoted verbatim in the confirmation file and verified against the stored evidence
  (sha `085d2103aac2…`), so **the roster (11 named trail sites) is genuinely confirmed** (`x2-verdict`'s
  per-fact check: 0 roster reasons). But `completionUnit` and `season` have **no explicit statement anywhere
  on rtjgolf.com** — checked `/faq/`, `/terms/` (rain-check policy only), `/loyalty/` (a rewards-points
  program, not a "complete the trail" rule), `/programs/`, `/trailcard/`, `/cambrianridge/` (maintenance
  schedule), and the four seasonal-package pages `/winter/` `/spring/` `/summer/` `/fall/` (which together
  imply year-round play via their date ranges, but never state a season window as a fact). No fabricated
  quote was written for either fact — per this task's honesty rule, they are recorded as **not found**, not
  guessed. RTJ is therefore unconfirmed (2 reasons: `completionUnit is missing`, `season is missing`).

**X2 overall verdict: KILL (0 of 3 confirmed, bar: ≥ 2).**

## VERDICT

**KILL.** Per X2.md's pre-written kill consequence: **swap in the reserve.** Order is OK first, then Hammock
Coast, then Kauai. **The swap is suspended pending the Addendum J re-check, by the owner's decision** (decision
0001 Addendum J, 2026-09-24, written after this Run 1 result was read) — see "Run 2 (under Addendum J)" below.
The reserve's own X2 fetch remains a follow-up, not assumed here, and is not triggered until the Addendum J
re-check of TN/VI/RTJ is complete and read.

## Log

| Date | Entry |
|---|---|
| 2026-09-23 | Memo created. Confirmed this session: CONNECT to `rtjgolf.com`, `tngolftrail.net`, `golfvancouverisland.ca`, `tn.gov` returns proxy 403; `WebFetch` also blocked. No fetch attempted or fabricated. |
| 2026-09-24 | Network unblocked. Ran `x2-fetch`/`x2-verdict` (build via `pnpm --filter @golfraven/p0-tools build`; evidence in `/tmp/golfraven-p0/x2-evidence-final/`). Added `tnstateparks.com/activities/golfing` and `rtjgolf.com/trail/` to `tools/p0/config/x2-sources.json` after finding TN's real content page (still WAF-blocked) and RTJ's actual roster page. **Measured: 0 of 3 confirmed → KILL.** TN indeterminate (site WAF block, all 3 configured hosts). VI fetched but is a JS-only SPA with no extractable roster/unit/season text; its Trail Pass Terms PDF now 404s. RTJ's 11-site roster confirmed verbatim against evidence; `completionUnit` and `season` not found anywhere on rtjgolf.com after checking 10+ pages — recorded as missing, not guessed. Swap rule applies: OK next (untested this session), needs owner confirmation before running X2 against the reserve trail. |
| 2026-09-24 | Decision 0001 Addendum J recorded (written after this Run 1 result was read — disclosed above). Ran `x2-fetch --render` against VI's two configured pages (evidence in `/tmp/golfraven-p0/run-j/x2-render-evidence/`, not committed). **VI now confirms** — see "Run 2 (under Addendum J)" below. TN and RTJ not re-run this session (TN needs Matt's owner-saved pages; RTJ's `completionUnit`/`season` are genuinely absent from rtjgolf.com regardless of fetch method, per Run 1). **X2 overall verdict stays pending TN** — not re-derived as KILL or PASS from VI alone. |
| 2026-09-24 | Opus gate review of the Addendum J tooling found 8 blocking findings; a dated correction was recorded in decision 0001 (Addendum J — correction). Tools fixed accordingly (`X2_RENDER_CHROMIUM_ARGS` allowlist, main-frame navigation control + off-host subresource blocking in `x2-render.ts`, first-capture-wins in `x2-ingest.ts`, method/httpStatus cross-check and non-recorded-capture refusal in `x2-verdict.ts`). VI's `completionUnit`/`season` quotes corrected (roster sentence for unit, Trail Pass Jan–Dec window for season) and re-verified against a fresh render taken under the fixed, off-host-subresource-blocking tool — same quotes present in both; the only content removed by the fix was third-party UI chrome (a map widget, a cookie banner), never anything cited. RTJ's four pages rendered under the first-capture rule to test whether Run 1's "completionUnit/season absent" conclusion was a direct-fetch artifact — it was not; still absent under rendering. See "Run 2 (under Addendum J)" below for full detail. |
| 2026-09-24 | Second Opus re-gate (at commit `24b2b6a`) found 3 more blocking findings, live-verified against real Chromium/a real git repo/an actual (non-reachable, see below) web.archive.org fetch attempt: (1) a dedicated Worker could open a same-host-disguised WebSocket that bypassed `context.routeWebSocket` entirely — fixed by failing the whole capture on ANY `page.on("worker")` event, plus a `Sec-Fetch-Dest: worker`/`sharedworker` request-level abort as defense in depth; (2) first-capture-wins could still be bypassed three ways — an owner-saved URL only had to share an ALLOWED HOST, not match a configured URL exactly (closed: exact-match-after-normalisation, itself extended to decode unreserved percent-encoding/collapse `//`/drop `;params`); the ledger matched on `(method, sha256)` alone, so a hand-edited row with a bogus `normalizedUrl` could still vouch for a non-recorded capture (closed: the match now also requires the manifest entry's own normalised URL to equal the ledger row's); and the per-directory default ledger path was silently re-openable by anyone who forgot `--ledger` (removed — `--ledger` is now REQUIRED for `x2-fetch`/`x2-ingest`/`x2-verdict`, and the canonical ledger now lives at [`docs/p0/x2-recorded-ledger.json`](x2-recorded-ledger.json), rebuilt from the TRUE earliest captures — see "Recorded-capture log" above — with a `git diff --quiet` cleanliness check gating `x2-verdict`'s own OFFICIAL/UNOFFICIAL read, `--allow-dirty-ledger` as the explicit escape hatch); (3) a `wayback` corroboration record's `snapshotText` was forgeable free text nobody ever checked against anything real — removed from the schema entirely; a new tool, `x2-corroborate-wayback.ts`, is now the ONLY way to make one, by actually fetching the snapshot, verifying its embedded URL/timestamp against the fact being corroborated, and storing the real bytes as evidence `x2-verdict` re-verifies (SHA recomputed, text re-derived with the same extractor) before trusting it; an `acceptance` record now also requires `acceptedBy` to be EXACTLY `"Matt"` and its `id`+`date` to be found in this file's own `## Log` section. **web.archive.org was checked and confirmed NOT reachable from this session's environment** (direct `fetch` and `curl`, with and without `NODE_USE_ENV_PROXY=1`, both failed — a connect timeout / connection reset through the agent proxy, not a code bug) — `x2-corroborate-wayback.ts` was built and tested entirely against an injected fetcher per its own design (see its own doc), never against the real network this session. Should-fix items also done: same-host WebSockets now call `connectToServer()` (a routed WebSocket does NOT connect to the real server by default — the prior "leave unhandled" comment was itself wrong, not just untested); a late popup/Worker re-check runs immediately before AND after `page.content()`; the rendered-evidence route now refuses any trail other than VI by default (an explicit `renderedAllowedTrails` override exists for a deliberate exception); this log's own "over four hours earlier" arithmetic error is corrected to ~38 minutes (see "Recorded-capture log" above), and that same section's Run 1 VI/RTJ timestamps are corrected to the TRUE earliest captures (~18:13Z, not ~18:18–19Z), with `tn.gov`'s two differing-content captures flagged as a CONFLICT. tools/p0 test count after this round: 590 passing (up from 543), `pnpm -r typecheck` clean. |
| 2026-09-24 | Third Opus re-gate (at commit `1f6729d`) found 4 more blocking findings, live-verified against real Chromium and real git repos with real `origin` remotes (never faked). (1) **SharedWorker/ServiceWorker bypass:** `page.on("worker")` never fires for a SharedWorker at all — `/blobshared` and `/netshared` probes leaked third-party WebSocket text past every prior defense. Fixed with two independent layers: an `addInitScript` installed in every frame, before any page script runs, that neutralises the `SharedWorker` constructor and `navigator.serviceWorker.register` at the JS-API level; and a browser-level CDP session (`Target.setDiscoverTargets`/`Target.setAutoAttach`) watching for ANY `shared_worker`/`service_worker`/`worker` target, failing the whole capture the instant one appears — independent of `page.on("worker")` entirely. Live-verified: `/blobshared`/`/netshared` now construct no SharedWorker at all (caught by the init script) and leak no third-party text; the CDP watch is independently proven (unit tests simulate a `Target.targetCreated` event with no `page.on("worker")` ever firing) and confirmed wired up against real Chromium (`Target.setDiscoverTargets`/`Target.setAutoAttach` actually sent, session detached on completion). (2) **Forged acceptance:** the old free-text `id` field let an agent write BOTH a corroboration record AND a plausible Log line naming its own made-up id. Fixed: `id` removed, replaced with a controlled `fact` identifier (`completionUnit`/`season`/`roster:<name>`) that must equal a STRUCTURED row, matched exactly (not a substring) inside `## Log`'s own bounded section — `ACCEPT <trail> <fact> <full evidenceSha256> <YYYY-MM-DD> Matt` — and `git blame` now finds the commit that introduced that row, requiring it reachable from `origin/main`, printing commit/author/date/`%G?` signature status. **Honest limit, stated here plainly:** agents in this environment act with Matt's own GitHub credentials, so no technical check can distinguish Matt's commit from an agent's — the real control is procedural, not technical: **agents must never write an `ACCEPT` row; only Matt adds one, by hand.** (3) **Hand-crafted Wayback record:** the verdict now re-validates every rule itself rather than trusting the record — URL form, embedded-URL match against the owner-saved fact's OWN stated URL, ±90-day tolerance against that fact's OWN `ownerSavedDate`, `rawFile` resolving inside the evidence dir to exactly `raw/<snapshotSha256>.<ext>` (a path-escape attempt like `../../outside.html` can never match this shape), the snapshot SHA DIFFERING from the owner-saved SHA (an identical SHA is the owner's own bytes relabelled), and the snapshot being registered in the canonical ledger under method `wayback` — `x2-corroborate-wayback.ts` now writes that ledger entry itself (and moved its raw-file naming to the plain `raw/<sha>.<ext>` shape this rule requires). Live-verified against the gate's own forged fixtures (`wb.json`'s self-referential SHA + bad URL form; `wb2.json`'s `../../outside.html` path escape) — both refused by the real built code, with the exact reasons named; a positive end-to-end run (real git repo, real `origin`, a genuine 3-row `ACCEPT` Log + a registered Wayback ledger entry) confirms the trail correctly PASSES when everything is legitimate. (4) **Ledger's git check too weak:** `checkLedgerAgainstGit` now requires ALL of: the ledger path resolves to exactly `<repo toplevel>/docs/p0/x2-recorded-ledger.json`; `git hash-object` on the file equals `git rev-parse origin/main:docs/p0/x2-recorded-ledger.json` (proves PUSHED, not just committed — if `origin/main` doesn't have the path yet, that specific case marks the result UNOFFICIAL and says why, rather than hard-refusing); `git ls-files -v` shows no `h`/`S` (assume-unchanged/skip-worktree) flag, which is refused unconditionally (never bypassable via `--allow-dirty-ledger`, since either flag exists specifically to hide a local edit from the very diff/status checks that flag is meant to override); and the last commit that touched the ledger is printed. Live-verified against the gate's own `g4/repo` fixture: its `ledger.json` sits at the wrong path (refused, "not the canonical ledger path") AND, once copied to the canonical path, is found to carry the `h` (assume-unchanged) flag hiding a genuinely different, forged 2-entry ledger from git's own diff/status — caught by the new flag check, exactly the attack this rule closes. **Should-fix items also done:** `extractX2MdLogSection` now stops at the NEXT `## ` heading (previously ran to end-of-file unconditionally, correct only because Log happened to be the last section); this file now states plainly, in the TN owner-saved combining section above, that web.archive.org is unreachable from this environment and names TN's two live paths (Matt's `ACCEPT` row, or a Wayback run from a machine that can reach archive.org). tools/p0 test count after this round: 612 passing across 22 test files (up from 590 across 22 — net +22, from new tests for these 4 findings plus a rewrite of the acceptance/wayback/ledger-git test blocks for the new schema), `tsc -b tsconfig.build.json` and root `pnpm -r typecheck` (run under Node 24.21.0, available this session) both clean. |
| 2026-09-24 | Fourth Opus re-gate (at commit `ad727ae`) found 1 more blocking finding: **"trust root is the caller's repo plus local refs."** Browser isolation (finding 1) and Wayback re-validation (finding 3) were confirmed still passing — this round touched only the ledger/acceptance trust chain. The prior round's fixes derived "the canonical path" from wherever `--ledger`/`--x2-log` happened to point (`git rev-parse --show-toplevel` run from the LEDGER'S OWN directory), and checked git reachability against the LOCAL, editable `origin/main`. Exploits A/A2/A3, all live-reproduced against this session's own build before the fix: a scratch `git init` repo, its own `docs/p0/x2-recorded-ledger.json` + `X2.md` (with `ACCEPT` rows hidden inside an HTML comment (A) or a fenced code block (A2), or a hand-written `method: "wayback"` ledger row for a self-authored file (A3)), committed "as Matt," then a bare `git update-ref refs/remotes/origin/main HEAD` — every one produced OFFICIAL / TN confirmed. Fixed three ways: **(a)** the trust root is now this TOOLKIT'S OWN checkout, resolved from `import.meta.url` (`resolveToolkitRepoRoot`), never from a caller-supplied path — `--ledger` must `realpath` to exactly `<this checkout>/docs/p0/x2-recorded-ledger.json`, `--x2-log` to exactly `<this checkout>/docs/p0/X2.md`, or the result is UNOFFICIAL (ledger) / the acceptance is never trusted at all (X2.md), full stop; **(b)** local refs are no longer trusted at all — every run deletes then freshly fetches `main` from the hard-coded canonical URL `https://github.com/mcorbett51090/GolfRaven` into `refs/x2-verdict/verified-main` (`fetchVerifiedMainRef`), and the ledger's blob hash / an `ACCEPT` row's `git blame`d commit are compared against THAT ref, never `origin/main`; a fetch failure gives UNOFFICIAL, never OFFICIAL (live-confirmed both ways: `git ls-remote`/a real `fetchVerifiedMainRef` run against `https://github.com/mcorbett51090/GolfRaven` from this environment succeeds and returns commit `4b4df751…`, while a deliberately-bad URL correctly fails closed); **(c)** only a VISIBLE `ACCEPT` row counts — `computeMarkdownLineVisibility` now marks a line hidden when it sits inside a fenced code block (``` ` ``` or `~~~`, matching-fence-required), an HTML comment (tracked across as many lines as needed), or an indented code block, and `findAcceptRowLine` skips every hidden line, including for the `## Log` heading search itself (so a fake heading hidden in a comment can't shift section boundaries). Should-fix, both done: an acceptance's `date` must be on/after the evidence's own `ownerSavedDate` and no more than 1 day after the row's own commit date; `docs/p0/X2.md`/`tools/p0/README.md`'s "proves PUSHED" wording is corrected — `%G?` and `git blame`'s author field prove nothing about identity in a shared-credential environment (a prior round overstated this), the real value is that a forgery now has to land in GitHub's own shared, public history for `main`, not a disposable local repo. Live re-verified against this session's build: exploits A and A2 (freshly reproduced, since the gate's own scratch fixture only covered A) now both produce `KILL`/`TN: No`/ledger `UNOFFICIAL`, with the exact "not this toolkit's own canonical docs/p0/X2.md" and "not the canonical ledger path" reasons; the gate's `g4/repo`-style wrong-path case is still caught the same way. A **path-depth bug** in the new `resolveToolkitRepoRoot` (two `..` segments instead of three, landing one directory short of the real repo root) was caught by this SAME live verification, not by the unit suite (whose tests all inject a test-only canonical-path override) — fixed and re-verified; the lesson recorded for next time: a security fix's OWN unit tests can miss a bug the tests' own injectable seam papers over, so the live end-to-end run against the real checkout is not optional. tools/p0 test count after this round: 642 passing across 22 test files (up from 612 — net +30: 7 new `resolveWaybackRecord` rule-by-rule tests, direct `computeMarkdownLineVisibility`/`findAcceptRowLine` unit tests, a rewritten trust-root/visibility/date-rule `acceptance` suite, a `fetchVerifiedMainRef` suite, and an expanded `checkLedgerAgainstGit` suite), `tools/p0`'s own `tsc -b tsconfig.build.json` and `tsc --noEmit -p tsconfig.json` both clean, root `pnpm -r typecheck` (Node 24.21.0) also re-confirmed clean. Live end-to-end run against the REAL checkout's own canonical `docs/p0/x2-recorded-ledger.json`/`docs/p0/X2.md` (no `ACCEPT` rows exist there yet): TN correctly reads pending/UNCONFIRMED ("No confirmation entry"), and the ledger reads UNOFFICIAL — not because the fetch failed (`verifiedMainFetch.ok: true`, `refs/x2-verdict/verified-main` resolved to `4b4df751…`, matching this session's own `git ls-remote` check), but because this checkout's own ledger blob is genuinely not yet on GitHub's real `main` (this session's work has never been pushed) — exactly the intended "OFFICIAL only if it matches GitHub main" behavior. |
| 2026-09-24 | Fifth Opus re-gate (Addendum J round 6, at commit `35f4be8`) found 1 more blocking finding: **"verification trusts the toolkit checkout's own local git state."** The fourth re-gate's fix (previous row) ran every GitHub-verification git command (fetch, blame, merge-base, rev-parse) INSIDE the toolkit's own checkout, with the caller's process.env inherited wholesale — so any local git-state manipulation available in that checkout, or in the calling process's environment, silently compromised the read. Three exploits, live-reproduced against this session's own build before the fix, each confirmed TN from a local, unsigned "Matt" commit: **(I)** `git config url.<fake>.insteadOf https://github.com/mcorbett51090/GolfRaven`, settable via local/global/SYSTEM config OR the GIT_CONFIG_COUNT/GIT_CONFIG_KEY_n/GIT_CONFIG_VALUE_n environment-variable channel (no config file needed at all), silently redirected the fetch to an attacker-controlled repo; **(R)** after a genuine fetch, `git replace <real> <forged>` made every git command transparently read the forged commit — defeated only by GIT_NO_REPLACE_OBJECTS=1, live-confirmed both ways (unprotected: substitution succeeds; GIT_NO_REPLACE_OBJECTS=1: it fails, confirming the cause); **(G)** a `.git/info/grafts` line rewrote a commit's parents, making a forged commit an ancestor of real main, live-confirmed the same way. Fixed by moving every GitHub-verification git command into a freshly created, disposable, BARE repository (`verifyAgainstGitHub`; mkdtemp + `git init --bare`, removed on exit) under an explicitly constructed, minimal environment — never `{...process.env}` — with HOME and GIT_DIR both pinned to that same disposable directory, GIT_NO_REPLACE_OBJECTS=1, GIT_CONFIG_NOSYSTEM=1, GIT_CONFIG_GLOBAL=/dev/null, and `-c core.hooksPath=/dev/null -c protocol.file.allow=never -c protocol.ext.allow=never` on every invocation (relaxed to `always` ONLY under a test-only `repoUrl` seam `main()` never passes). A `git ls-remote --get-url` canary (resolves git's own config-rewriting logic with no network connection) catches any surviving insteadOf redirection before the fetch; full history is fetched (no `--depth`) and a shallow result refused (`--is-shallow-repository`); `info/grafts`/`refs/replace/*` in the fresh repo are refused outright as belt-and-braces; GIT_SSL_NO_VERIFY in the original environment, or `http.sslVerify=false` resolved from it, both refuse before any fetch. Content authority also moved to GitHub: the ledger blob AND X2.md's own text are now read with `git show <ref>:<path>` inside the disposable repo, never the local working tree (neutralising a symlinked-docs/p0 trick too) — `git blame` runs directly against `refs/heads/main` there, so reachability is true BY CONSTRUCTION and the old separate `merge-base --is-ancestor` step is gone entirely (one fewer command to subvert). **Should-fix, both done:** a Wayback corroboration now also requires THIS RUN's own ledger to be OFFICIAL (`resolveWaybackRecord` gates on a new `ledgerOfficial` flag threaded from `main()`) — round 5's exploit A3 (a self-authored `method: "wayback"` ledger row plus local ref forgery) still reported `waybackVerified: true` under an UNOFFICIAL ledger header before this; and `computeMarkdownLineVisibility` now also hides any line inside a raw HTML block (CommonMark's own block-tag rule — `<details>`, `<div>`, `<table>`, etc., ending at the next blank line) or carrying a `hidden`/`style` attribute, closing the same class of forgery the fence/comment rules already closed. Live-verified against the reviewer's own round-6 fixtures (`/tmp/gr-gate2/probe/g6/`, including its `fake.git` forged-commit repo) and against the real dist: with the SAME forged local ledger+X2.md content the fake.git repo carries, a baseline run (real GitHub, no exploit) and an exploit-I run (GIT_CONFIG_COUNT/KEY/VALUE redirecting the real URL to fake.git, confirmed to work against an unprotected `git ls-remote --get-url` first) produced IDENTICAL results — TN: No, acceptance "not found/not pushed" against GitHub main's own text, ledger UNOFFICIAL — proving the redirect had no effect on the real dist. tools/p0 test count after this round: 658 passing across 22 test files (up from 642 — net +16: a `should-fix ledgerOfficial` pair, a fully rewritten `acceptance`/`checkLedgerAgainstGit` suite against real local bare "GitHub stand-in" repos via `verifyAgainstGitHub({repoUrl})`, and new `verifyAgainstGitHub` describe blocks for exploits I/R/G, GIT_DIR-in-caller-env, `.git/hooks`, a shallow source, and GIT_SSL_NO_VERIFY), `tools/p0`'s own `tsc -b tsconfig.build.json` and `tsc --noEmit -p tsconfig.json` both clean. Live end-to-end run against the REAL checkout's own canonical `docs/p0/x2-recorded-ledger.json`/`docs/p0/X2.md` (run from a /tmp mirror whose `docs/` is a symlink to the real checkout, so the canonical-path check genuinely resolves to the real files without writing into the real checkout at all): `githubVerification.ok: true` (real network fetch to `https://github.com/mcorbett51090/GolfRaven` succeeded), TN correctly reads pending ("No confirmation entry"), and the ledger reads UNOFFICIAL — "GitHub main does not yet have docs/p0/x2-recorded-ledger.json" — because this branch's work has never been pushed to main, exactly the intended behavior. |
| 2026-09-24 | Sixth Opus re-gate (Addendum J round 7, at commit `cdec7b7`) found 1 more blocking finding, narrow: **"an agent that controls the verdict process's runtime environment still gets OFFICIAL with a forged acceptance."** The fifth re-gate's fix (previous row) closed every LOCAL GIT-STATE forgery class, but still (a) resolved the literal string `"git"` through PATH — a fake `git` shim placed earlier on PATH, or PATH itself rewritten, was silently trusted, since `execFileAsync("git", …)` does a PATH lookup every time; and (b) ran inside the SAME Node process the caller launched, so `NODE_OPTIONS="--require evil.cjs"`, `LD_PRELOAD`, or any comparable mechanism that injects code into that process could patch `child_process.execFile` before a single line of this module's own code ever ran, making every "scrubbed" git invocation transparently go through attacker code regardless of what env/args this module constructed for it. The gate's own fixtures (`fakebin/git`, `evil.cjs`) demonstrated both live: `fakebin/git` rewrites the real GitHub URL to a forged local repo while lying to the `ls-remote --get-url` canary; `evil.cjs` patches `child_process.execFile` itself the same way, confirmed this round to redirect an unprotected `git ls-remote --get-url` when loaded via `NODE_OPTIONS="--require evil.cjs"`. Fixed three ways: **(1)** `resolveGitBinary` resolves `git` to a verified ABSOLUTE path (`/usr/bin/git`, falling back to `/bin/git`), refusing outright — never silently trying the other candidate — unless it is owned by root and not group-/world-writable; a `gitBinary` override is a TEST-ONLY seam that still runs the SAME checks against the given path, never bypassing them, so a test can prove both a trustworthy override validates and a deliberately bad one (wrong uid, or group/world-writable — constructed via `chownSync`/`chmodSync` in this root-run test environment) is genuinely refused. **(2)** every child git process's own `PATH` is fixed to `/usr/bin:/bin` (`FIXED_CHILD_PATH`), never the caller's — applied to BOTH the disposable-repo GitHub-verification commands and `checkLedgerAgainstGit`'s separate local cleanliness checks (hash-object/diff/status/ls-files/log), since a fake git reachable during those could equally have lied about a local blob hash matching GitHub's real one. **(3)** `detectRuntimeTamper` (injected `env`/`execArgv`, never reading `process.env`/`process.execArgv` directly — the seam is `verifyAgainstGitHub`'s `runtimeEnv`/`runtimeExecArgv` options, which `main()` never overrides) refuses (UNOFFICIAL) when the verdict process's OWN original environment carries `LD_PRELOAD`/`LD_LIBRARY_PATH`/`GIT_EXEC_PATH`/any `DYLD_*` variable at all, `NODE_OPTIONS` carrying a code-loading flag (`--require`/`-r`/`--loader`/`--experimental-loader`/`--import` — deliberately NOT a blanket "NODE_OPTIONS is set" refusal: this environment's own ordinary shell sets a benign `--max-old-space-size` NODE_OPTIONS, confirmed live this round, and a blanket refusal would have made the CLI falsely UNOFFICIAL on every normal run here), or `process.execArgv` being non-empty (empty for a plain `node dist/x2-verdict.js` file invocation, confirmed live this round; vitest's OWN worker process carries a non-empty execArgv including its own `--require suppress-warnings.cjs`, also confirmed this round, which is why every pre-existing test calling the real `verifyAgainstGitHub` now passes an explicit `runtimeExecArgv: []` override — the seam exists for exactly this). **Follow-up, done:** `<details>…</details>` is now hidden across blank lines (a stricter rule than every other raw-HTML-block tag, which still ends at the first blank line per CommonMark) — GitHub's own renderer keeps a `<details>` section collapsed across internal paragraph breaks, so the generic rule would have let a forged row after such a blank line read as "visible" here while still inside the collapsed section on GitHub. Live-verified against the reviewer's own round-7 fixtures (`/tmp/gr-gate2/probe/g7/fakebin/git`, `evil.cjs`) and against the real dist, using the SAME forged local ledger+X2.md content and a freshly reconstructed `fake.git` (the original was gone, recreated with matching forged `ACCEPT` rows + ledger entry so the fixtures had something real to redirect to): a baseline run and a `fakebin/git`-shimmed-first-on-PATH run produced IDENTICAL results (TN: No, acceptance "not found/not pushed" against GitHub main's own text); `NODE_OPTIONS="--require evil.cjs"` produced `githubVerification.ok: false` with the exact NODE_OPTIONS reason, before evil.cjs's patched `execFile` ever got a chance to run. tools/p0 test count after this round: 685 passing across 22 test files (up from 658 — net +27: `resolveGitBinary`/`detectRuntimeTamper` direct unit tests, a `verifyAgainstGitHub`-wired round-7 hardening suite covering the fake-PATH-shim/NODE_OPTIONS/LD_PRELOAD/execArgv/non-root-git/fixed-child-PATH cases, and `<details>`-across-blank-lines `computeMarkdownLineVisibility` tests plus an end-to-end acceptance-resolution case), `tools/p0`'s own `tsc -b tsconfig.build.json` and `tsc --noEmit -p tsconfig.json` both clean. Live end-to-end run against the REAL checkout's own canonical `docs/p0/x2-recorded-ledger.json`/`docs/p0/X2.md`, from a genuinely clean shell (`env -i`, stripping even this environment's own ambient `NODE_OPTIONS`) AND from this environment's normal ambient shell (which DOES carry that benign `NODE_OPTIONS` — both give the same result, proving the round-7 check does not falsely trip on it): `githubVerification.ok: true`, TN reads pending ("No confirmation entry"), and the ledger reads UNOFFICIAL — "GitHub main does not yet have docs/p0/x2-recorded-ledger.json" — because this branch's work has never been pushed to main, exactly the intended behavior. The Scope statement above is updated accordingly: it no longer claims "environment-variable manipulation is defended in general" (the prior wording's own overclaim, per this round's finding) — only the specific channels this round actually closes. |
| 2026-09-24 | Seventh Opus re-gate (Addendum J round 8, at commit `c20d53c`) found 1 more blocking finding, docs-only: **round 7's own Scope statement made a false claim.** It listed NODE_OPTIONS loader flags and non-empty execArgv under "defends against." They are not, and cannot be, defended against by any in-process check: the round-8 gate fixture extended `evil.cjs` with `delete process.env.NODE_OPTIONS; process.execArgv.length = 0;` at its very top, run BEFORE the rest of its payload and therefore BEFORE `detectRuntimeTamper` (x2-verdict.ts:1415-1465 at that commit) ever executes — the preload erases the evidence the check looks for before the check can look. Live-confirmed this round: with the self-erasing preload, the verdict came back OFFICIAL with TN confirmed — the exact same result as no attack at all. This is structural, not a bug: NODE_OPTIONS preload code is defined to run before a single line of this module's own code, so no ordering this module could choose ever runs first. **No code change closes this** — `detectRuntimeTamper` is downgraded, honestly, to a TRIPWIRE FOR NAIVE, NON-SELF-HIDING injection only, kept as defense in depth (confirmed this round: the SAME evil.cjs payload, without the self-erasing line, is still refused). The Scope statement is rewritten accordingly: NODE_OPTIONS loader flags, execArgv, and LD_PRELOAD/LD_LIBRARY_PATH/LD_AUDIT/GIT_EXEC_PATH/DYLD_* are moved OUT of the "defends against" list; residual #1 is extended to cover any code injected into the verdict process (including through the listed variables, since injected code runs first and can erase the indicators), and now names NODE_EXTRA_CA_CERTS (a TLS-trust residual — it's on this module's own proxy/CA allow-list for legitimate reasons, but an agent who controls it controls a trust anchor for the "verified against GitHub" fetch) and NODE_PATH (a module-resolution-hijack residual — the same injection class as NODE_OPTIONS/--require, via a different, uncheckable-from-inside mechanism) explicitly rather than leaving them implicit. **Follow-ups, both done, kept as defense in depth for the naive-attempt case specifically:** (1) `LD_*` is now a PREFIX check, not a fixed two-name list — the gate's own probe used `LD_AUDIT` (untouched by round 7's `LD_PRELOAD`/`LD_LIBRARY_PATH` list) to load a native shared object into the process; live-confirmed this round, both that the library's constructor genuinely ran (it left its own marker file) and that `detectRuntimeTamper` now catches `LD_AUDIT` specifically. (2) NODE_OPTIONS moved from a DENYLIST of known-dangerous flags (round 7's own `--require`/`-r`/`--loader`/`--experimental-loader`/`--import` list, which never named `--inspect*`, `--env-file`, `--conditions`, `--openssl-config`/`--use-openssl-ca`, `--preserve-symlinks*`, or any `--experimental-*` flag) to an ALLOW-LIST of known-benign ones (`--max-*`, `--stack-size`, `--unhandled-rejections=*`, `--no-warnings`, `--enable-source-maps`, `--trace-warnings`), via a proper tokenizer (`tokenizeNodeOptions`, handling quoted values and both `--flag=value`/`--flag value` forms) plus `isNodeOptionTokenAllowed`, both newly exported and directly unit-tested; this environment's own ambient `--max-old-space-size=8192` still passes, confirmed live this round. Live-verified against the reviewer's own round-8 fixtures (`/tmp/gr-gate2/probe/g8/`: `evil.cjs` with the self-erasing preload, `audit.c`/`LD_AUDIT`) and against the real dist, using the SAME forged local ledger+X2.md content and a freshly reconstructed `fake.git` (the original was gone, recreated with matching forged `ACCEPT` rows + ledger entry): `LD_AUDIT` is refused (UNOFFICIAL, exact LD_AUDIT reason) even though the native library genuinely loaded; the self-erasing `evil.cjs` still produces OFFICIAL/TN-confirmed (the accepted, undefendable residual, exactly as now documented); the SAME payload without the self-erasing line is still refused by the new allow-list (defense in depth against the naive shape still works). tools/p0 test count after this round: 698 passing across 22 test files (up from 685 — net +13: `tokenizeNodeOptions`/`isNodeOptionTokenAllowed` direct unit tests, and `detectRuntimeTamper` tests for LD_AUDIT, `--inspect`/`--inspect-brk=0`/other unvetted flags, and the full benign allow-list both individually and combined), `tools/p0`'s own `tsc -b tsconfig.build.json` and `tsc --noEmit -p tsconfig.json` both clean. Live end-to-end run against the REAL checkout's own canonical `docs/p0/x2-recorded-ledger.json`/`docs/p0/X2.md`, from this environment's normal ambient shell (NODE_OPTIONS=--max-old-space-size=8192 present, unaffected): `githubVerification.ok: true`, TN reads pending ("No confirmation entry"), ledger reads UNOFFICIAL — "GitHub main does not yet have docs/p0/x2-recorded-ledger.json" — because this branch's work has never been pushed to main, exactly the intended behavior. |

---

## X2 Run 2 (under Addendum J) — 2026-09-24, VI part only (partial)

Re-run **after** decision 0001 Addendum J(a) — the owner's decision, made after reading the Run 1 result
above — added a `rendered` evidence route for a page whose static HTML carries no body text (VI's case,
exactly) and an `owner-saved` route for a page a direct fetch cannot reach at all (TN's case). Run 1's result
above is left on the record unedited.

**Scope: VI confirmed; RTJ's absence assumption tested (render, not a new confirmation attempt); TN still
waiting on Matt's owner-saved pages.** TN is still waiting on Matt's owner-saved pages (Addendum J(a)(ii)) —
not run this session. RTJ's roster is already confirmed (Run 1); its `completionUnit`/`season` were RE-TESTED
by rendering four of its pages under the first-capture rule (§"RTJ render" below), specifically to check
whether Run 1's "genuinely absent" conclusion — reached via direct fetch only — still held once rendering
removed "the fetch method itself might be the problem" as a possible explanation. It did. **No new overall X2
verdict is derived from VI alone — it stays pending TN, per this task's explicit instruction.**

**Note on this section's own quotes:** the `completionUnit` and `season` quotes originally drafted for this
run (citing "ten (10) participating golf facilities" and the homepage's "mild coastal climate… year-round"
sentence) were corrected after a gate review — see below for the reasoning and the corrected quotes actually
used in the confirmation file `x2-verdict` checked. The facts they cited are still real, verbatim text on the
page; the correction is about *which* sentence best settles each fact, not about fabricating anything.

### METHOD

```shell
node dist/x2-fetch.js --render --config <VI-only config> --out-dir /tmp/golfraven-p0/run-j/x2-render-evidence
```

`--render` (decision 0001 Addendum J(a)(i)) boots headless Chromium at `/opt/pw-browsers/chromium` against
VI's two configured pages (`golfvancouverisland.ca/` and `golfvancouverisland.ca/about-us/golf-trail-pass/`),
keeping the tool's own bot-identifying User-Agent. **Environment note, not a tooling change:** this session's
own agent proxy re-terminates TLS with a locally-issued CA that Chromium does not trust by default (Node's
own `fetch`, used by the rest of this tool, already trusts it via `NODE_EXTRA_CA_CERTS`) — resolved by passing
`X2_RENDER_CHROMIUM_ARGS` with `--ignore-certificate-errors-spki-list=<this session's own known proxy CA
SPKI hashes>`, a scoped pin of specific already-known certificates, not a blanket disabling of TLS
verification, and nothing proxy-specific was added to the tool's source (see `x2-render.ts`'s `extraArgs`).

### RESULT — does the rendered text now contain roster, completionUnit and season text?

**Yes to all three**, verified by `x2-verdict` itself against the real rendered evidence (not just read by
eye) — full command output in `/tmp/golfraven-p0/run-j/x2-verdict-vi-only/result.md`. Both pages rendered
cleanly (HTTP 200, no redirect): the homepage's extracted text is now **20,114 characters** (`sha256
731ca853fcab…`, was 69 bytes — just the `<title>` — under a direct, non-rendered fetch in Run 1) and the
Trail Pass page's is **15,661 characters** (`sha256 5869018ffb2e…`, was 80 bytes in Run 1).

- **Roster — present.** The Trail Pass page names 10 courses in one sentence (`sha256 5869018ffb2e…`, 329
  characters):

  > "Passholders can redeem their card to play one round at each of the following participating Vancouver
  > Island golf courses through the 2026 golf season. Courses include: Arbutus Ridge * Bear Mountain – Valley
  > Course * Campbell River * Cowichan * Crown Isle * Fairwinds * Highland Pacific * Morningstar * Olympic
  > View * Storey Creek"

  All 10 names verified verbatim by `x2-verdict` against this evidence. This is close to, but not identical
  to, `research/canada-trails.md`'s "11–13, conflicting" — this page names exactly 10 Trail-Pass-participating
  courses (naming only the Valley Course at Bear Mountain, not both Bear Mountain courses separately).

- **completionUnit — settled as `course`, on the roster sentence itself** (`sha256 5869018ffb2e…`, the SAME
  329-character sentence quoted for roster above — reused, not a new quote):

  > "Passholders can redeem their card to play one round at each of the following participating Vancouver
  > Island golf courses through the 2026 golf season. Courses include: Arbutus Ridge * Bear Mountain – Valley
  > Course * Campbell River * Cowichan * Crown Isle * Fairwinds * Highland Pacific * Morningstar * Olympic
  > View * Storey Creek"

  This sentence itself says "golf **courses**", and the roster it introduces names Bear Mountain's **"Valley
  Course"** specifically — one of Bear Mountain's two courses, not "Bear Mountain" as a whole facility — which
  only makes sense if the unit being counted is the individual course. **Caveat, recorded honestly, not
  smoothed over:** the SAME page separately states, one paragraph later (81 characters):

  > "This pass can be redeemed for play at the ten (10) participating golf facilities."

  — calling the identical 10 entities "golf **facilities**" instead. The roster sentence is used as the
  settling quote because it is the one that actually enumerates what is being counted (specific courses, one
  of them named at the sub-facility level); the "facilities" wording is recorded here as a real, page-internal
  inconsistency, not discarded.

- **season — settled as the Trail Pass's own redemption window, on the Trail Pass page** (`sha256
  5869018ffb2e…`, 71 characters):

  > "This pass can be redeemed for play between Jan 1 and December 31, 2026."

  — i.e. redeemable across the full calendar year, with this caveat quoted alongside it, one sentence later in
  the same evidence (97 characters):

  > "Please check with courses before arriving, as course closures may occur during the winter months."

  So the Trail Pass itself has no BLACKOUT season (redeemable Jan–Dec), but individual courses may still close
  seasonally — recorded as the actual, more precise fact, in place of the homepage's looser "we really do play
  year-round" marketing language used in an earlier draft of this run.

**Separately, a product note, not part of either settled fact** — the Trail Pass page also states (69
characters, `sha256 5869018ffb2e…`):

> "The Golf Trail Pass is only available for Vancouver Island Residents."

This bears on who can actually BUY the pass (a booking/marketing fact), not on the trail's roster,
completion unit, or season — recorded here for completeness, not folded into any of the three X2 facts.

**VI trail-level result under `x2-verdict` (this partial run, evaluated alone — see caveat below):
CONFIRMED**, all three facts backed by verbatim quotes in the rendered evidence, `method: "rendered"` carried
through on every fact in the tool's own output. Full command output in
`/tmp/golfraven-p0/run-j/x2-verdict-vi-stepc/result.md`.

**Caveat, stated per the task's own instruction:** `x2-verdict`'s `overallVerdict` field for a VI-only
confirmation run necessarily reads KILL (1 of 3 confirmed against the ≥ 2 bar) — that field is **not** the
recorded X2 verdict. The recorded X2 overall verdict is **pending TN owner-saved pages** (see below), not
re-derived from VI alone.

### Re-verification against the new first-party check (gate finding)

A gate review found that the render path could surface text a THIRD PARTY injected (a script loaded from a
different host), not just the trail site's own content — `x2-render.ts` was fixed to abort every off-host
subresource request outright (the gate's own preferred "primary fix"). The captures cited above
(`731ca853fcab…` / `5869018ffb2e…`) were taken BEFORE that fix existed, so they were re-verified, not just
assumed clean:

- A **fresh** render of the same two VI URLs was taken under the FIXED tool (off-host subresources blocked,
  main-frame navigation host/https-checked) — `/tmp/golfraven-p0/run-j/x2-render-evidence-verify/` (not
  committed; not the recorded capture — first-capture-wins means the original `731ca853fcab…` /
  `5869018ffb2e…` captures stay the recorded ones regardless of this check's outcome).
- **Every quote cited above (roster, completionUnit, season, the residency note) is present, verbatim, in the
  fresh capture too** — checked programmatically, not by eye.
- **The fresh capture's homepage text is genuinely different from the original — 19,414 characters vs. 20,114
  — and the diff is exactly the kind of thing the fix is meant to remove**, not a fact going missing: an
  embedded map widget's keyboard-shortcut accessibility text (`"← Move left → Move right ↑ Move up…"`,
  characteristic of a Google Maps embed) and a cookie-consent banner's text (`"This website uses
  cookies…"`) are present in the OLD capture and ABSENT from the NEW one. Both read as third-party-served
  UI chrome, not trail content, and neither ever appeared in any of this run's cited quotes.
- **Conclusion: the recorded captures PASS the new first-party check** — every fact this run relies on is
  present with or without off-host subresource blocking, and the off-host-only content the blocking removed
  was UI chrome, not anything cited here. The primary fix (abort off-host subresources) does not break VI's
  rendering and was not swapped for the fallback first-party-response-body approach — the primary fix alone
  was sufficient, confirmed live.

### RTJ render — testing whether `completionUnit`/`season` are genuinely absent (not a new confirmation attempt)

Run 1 concluded `completionUnit` and `season` are missing from `rtjgolf.com` via DIRECT fetch, after checking
10+ pages. Since rendering found content on VI that a direct fetch could not read at all, the same question
had to be asked of RTJ: is `rtjgolf.com`'s "nothing here" also a fetch-method artifact, or is the absence
real? Four pages were rendered under the first-capture rule (first render of each of these four URLs, so
each is now that URL's own recorded capture): `rtjgolf.com/`, `/trail/`, `/faq/`, `/terms/` — evidence in
`/tmp/golfraven-p0/run-j/x2-render-evidence-rtj/` (not committed).

**Honest result: still absent, even rendered.** `rtjgolf.com/` itself rendered to only 98 characters (`"Robert
Trent Jones Golf Trail About the Trail Build My Trip The Trail Build My Trip Tee Times Shop"`) — a thin nav
shell, RTJ's own version of VI's problem, but with no `data-page`-style JSON payload found in it either; the
other three pages rendered normally (5,900–7,146 characters each) and contain real content — cancellation/
rain-check policy, trail-site mileage, booking FAQs — but neither a completion-unit rule ("play all 11 sites",
"visit every course") nor a season/blackout-window statement appears anywhere across all four pages. The only
season-adjacent hit is unrelated: *"If the course is closed due to rain the Pro Shop will issue you a rain
check for the round"* — a weather-cancellation policy, not a season window. **This confirms Run 1's
conclusion under a second fetch method, not just the first one — the absence looks real, not a direct-fetch
artifact.** RTJ's roster (Run 1) is unaffected by any of this. No confirmation file was written for this
render — it answers a factual question (does content exist that direct-fetch missed?), it is not an attempt
to newly confirm RTJ.

### Recorded-capture log (gate finding 3): full SHA-256 + UTC time, and a disclosed procedural slip

Gate finding 3 asked for the full SHA-256 and UTC retrieval time of every recorded capture cited above, not
just the truncated (12-char) SHAs used inline — logged here, read straight from the actual `manifest.json`
files (`fetchedAt`, UTC ISO-8601), not retyped from memory:

**Correction (Addendum J re-gate, second round): the rows below were originally sourced from
`x2-evidence-final/manifest.json` (fetched ~18:18–18:19Z), which is NOT the earliest real capture of most of
these URLs — an earlier, separate run (`run2/x2-evidence/manifest.json`, ~18:13Z) captured several of the same
URLs first. Corrected below to the TRUE earliest capture per URL+method, verified by comparing `fetchedAt`
across every manifest that captured it, not just the one this log happened to look at first.**

| Capture | URL | Full SHA-256 | UTC time (`fetchedAt`) |
|---|---|---|---|
| VI — recorded, homepage (rendered) | `golfvancouverisland.ca/` | `731ca853fcab032374de0ca367869dff005905d60fb597fdc46d8966d71b3fbc` | 2026-09-24T18:57:14.555Z |
| VI — recorded, Trail Pass page (rendered) | `golfvancouverisland.ca/about-us/golf-trail-pass/` | `5869018ffb2edb3a4f0c80e5fbb6cb2b6be2d053e41e35f3db4bff2a073f5ae4` | 2026-09-24T18:57:26.575Z |
| RTJ — render, homepage | `rtjgolf.com/` | `dbc7914fa4a67f2e137f4789d7f2e44390cd1799a1bbf987bede03c098099984` | 2026-09-24T19:32:09.462Z |
| RTJ — render, `/trail/` | `rtjgolf.com/trail/` | `cc0754225103557e48af99e02cce8664b99d264d863d7a6af664bb538c64ce7c` | 2026-09-24T19:32:12.951Z |
| RTJ — render, `/faq/` | `rtjgolf.com/faq/` | `1a48f3517dc709be4680382555eb173f0563ff6b59f1840448905fb9a63c0e17` | 2026-09-24T19:32:16.081Z |
| RTJ — render, `/terms/` | `rtjgolf.com/terms/` | `e6f8f1cd35ed0c738620be1c7320da365db5b9ada44beafc32d4c1b05767235f` | 2026-09-24T19:32:18.298Z |
| Run 1 — direct, RTJ `/trail/` (roster evidence — only capture, no earlier one exists) | `rtjgolf.com/trail/` | `085d2103aac2fa520b3c1d52c518934845725465cb34dbd06861e44d772b34d8` | 2026-09-24T18:19:00.016Z |
| Run 1 — recorded, direct, RTJ homepage | `rtjgolf.com/` | `e294c864cdd53fdd8d194ba7f3fef046b05ab1b278c4d62d58b42bfcc9a0616a` | **2026-09-24T18:13:40.798Z** |
| Run 1 — recorded, direct, VI homepage | `golfvancouverisland.ca/` | `efe829bc5301405bce35c7112c09aa51ea279865d2fe57a94ce0e4819513821d` | **2026-09-24T18:13:39.750Z** |
| Run 1 — recorded, direct, VI Trail Pass page | `golfvancouverisland.ca/about-us/golf-trail-pass/` | `679670f8291d7afcab1b59f8b71bb4e9aa586da2429e2f3462326c4692d41f31` | **2026-09-24T18:13:40.067Z** |
| Run 1 — recorded, direct, TN `tn.gov` (not cited toward any fact) | `tn.gov/` | `7270893e3f0188f0ded07ee11ac292aadbeab41d34d28909156c73cb70554a0a` | **2026-09-24T18:13:38.178Z** |
| Run 1 — LATER, non-recorded, direct, TN `tn.gov` — **CONFLICT, not a mere duplicate** | `tn.gov/` | `3cfc8f9b014087837b151d619b846927f9483c22086204abd846aae44eee23e3` | 2026-09-24T18:18:56.895Z |

**The `tn.gov` row is flagged CONFLICT, not just "a later capture," because its bytes genuinely differ from
the 18:13 capture** (`3cfc8f9b…` ≠ `7270893e…` — two different SHA-256 values for the same URL, same method,
~19 minutes apart) — most likely the state portal's own page changing between fetches (a news/nav-heavy
government homepage), not a tooling bug. First-capture-wins still applies mechanically the same way it would
for an identical-content duplicate — `7270893e…` is the recorded one, `3cfc8f9b…` is not — but disclosing
"the content actually changed" here matters more than for VI/RTJ's duplicates (whose later re-captures are
byte-for-byte identical to the first, confirmed by the matching SHA-256s in the table above). Neither `tn.gov`
capture is, or was, cited toward any X2 fact — Run 1's own STATUS already recorded that `tn.gov`'s content has
no golf-trail roster/unit/season text — so this conflict changes no verdict; it is logged here purely for gate
finding 3's completeness.

**The VI/RTJ direct-capture ROW TIMES above are corrected from an earlier version of this log**, which cited
`x2-evidence-final/manifest.json`'s later (~18:18–18:19Z) captures of the SAME URLs as if they were the first.
They are not: `run2/x2-evidence/manifest.json` captured the identical content (same SHA-256 in every case
except `tn.gov`, above) 18–19 minutes earlier, at ~18:13Z. The SHA-256 values in this log were already correct
(the content is identical either way); only the claim "this is the recorded/first capture" needed the earlier
timestamp. See the "Ledger note" below for the canonical ledger file now built from these corrected times.

**RTJ's four renders are logged here as an absence-test result, not as confirmation evidence** — consistent
with "RTJ render" above: rendering was never part of Matt's own choice for RTJ (Addendum J(a)(i)'s render
route was adopted for VI's static-HTML-has-no-body-text problem; the correction recorded in decision 0001
makes clear the render route's inclusion in Matt's decision covered VI, not RTJ). These four SHAs answer a
factual question (does rendering surface content a direct fetch missed?) — no confirmation file cites them,
and none should.

**Disclosed procedural slip: this log was written AFTER the fact.** `vi-confirmation-stepc.json` — the
confirmation file `x2-verdict` actually ran against — cites evidenceSha `5869018ffb2edb3a4f0c80e5fbb6cb2b6be2d053e41e35f3db4bff2a073f5ae4`
for all three VI facts, and that citation was made (and `x2-verdict` was run against it) *before* this table
existed. The correct order would have been: log the recorded capture first, cite it second. It wasn't, here —
disclosed plainly rather than silently backfilling the table and leaving no trace of the ordering. **This
does not, on its own, make the citation wrong** — checked here, not assumed: the cited SHA
(`5869018ffb2e…`) is genuinely the FIRST capture of that URL. Two renders of
`golfvancouverisland.ca/about-us/golf-trail-pass/` exist in this session's own evidence —
`5869018ffb2edb3a4f0c80e5fbb6cb2b6be2d053e41e35f3db4bff2a073f5ae4` at **2026-09-24T18:57:26.575Z** (the
original capture, cited) and `607f27982ce2e523dfbe593a411499b9db5030aadceb670256884d813f6cd37e` at
**2026-09-24T19:31:22.940Z** (the later first-party-check re-verification capture, "Re-verification against
the new first-party check" above) — 18:57:26.575Z is earlier than 19:31:22.940Z, so the cited SHA is indeed
the first capture, and the later `607f2798…` one (along with the homepage's later `7b9eb886…`) is correctly
the non-recorded one; see the ledger note immediately below for how that is now enforced mechanically rather
than left to this manual timestamp comparison.

**Ledger note — backfilled, corrected, and now CANONICAL, not left as a promise.** These captures predate this
session's recorded-captures ledger (`x2-recorded-ledger.ts`, gate finding 2c) — none of the
`/tmp/golfraven-p0/...` manifests above were written under it, so their own `recorded` fields (where present at
all) are the pre-fix, hard-coded values gate finding 2c flagged, not a ledger-backed determination. **Gate
finding 2d (re-gate): the reconstructed ledger now lives in the repo itself, at
[`docs/p0/x2-recorded-ledger.json`](x2-recorded-ledger.json)** (SHAs, URLs and UTC times only — no PII), built
from the CORRECTED true-earliest-capture table above (this is also where the `tn.gov`/VI/RTJ timestamp
correction actually matters mechanically, not just in this log's prose). **This pass itself never ran `git
commit`** (the rules for this task were no commit/push) — but this repo's own environment periodically
checkpoints the working tree on its own (observed this session as `wip: in-progress agent work` commits, not
something this pass triggered), and that checkpoint landed this file: `git diff --quiet -- docs/p0/
x2-recorded-ledger.json` now reports clean, checked directly rather than assumed. Running `x2-verdict` against
it (no `--allow-dirty-ledger` needed) prints `Ledger: …/x2-recorded-ledger.json (blob
b5d87a105d7f0eddf09489908da183e294c343cd) — OFFICIAL (clean in git)` — gate finding 2d's own mechanism, not
hand-verified. If a future session finds this file dirty again (a genuine hand-edit, or an environment without
this same auto-checkpoint behavior), `--allow-dirty-ledger` remains the documented escape hatch, marking that
run's output UNOFFICIAL until a real, deliberate commit lands.
The ledger match is now on **method, SHA-256, AND normalised URL together** (gate finding 2b, re-gate — a
hand-edited ledger row with the right method+SHA but a bogus/mismatched `normalizedUrl` no longer matches;
verified live against the gate's own such attempt, `edited-ledger.json`, below) — a correction from an earlier
version of this note, which said the match was on `(method, sha256)` alone. Run against real directories to
check the claim, not just asserted:

- `x2-verdict --evidence-dir .../x2-render-evidence-verify --confirmation <a test file citing 607f2798…>
  --ledger <the backfilled ledger>` — **refuses to run**, with: `"...cites evidenceSha
  \"607f27982ce2e523dfbe593a411499b9db5030aadceb670256884d813f6cd37e\", which is a NON-RECORDED capture..."`
  — confirming `607f2798…` (and by the same construction, the homepage's `7b9eb886…`) is now mechanically
  `recorded: false`, exactly as this gate finding required, not by manual inspection.
- `x2-verdict --evidence-dir .../x2-render-evidence --confirmation vi-confirmation-stepc.json --ledger <the
  same backfilled ledger>` — **still confirms VI** (`VI: Yes, roster size 10`, `X2 overall verdict: KILL (1 of
  3 confirmed, bar: ≥ 2)` — same per-trail result as before the ledger was added, since `5869018ffb2e…` is
  genuinely the recorded capture). The ledger closes the gate finding without disturbing VI's actual result.
- `x2-verdict --evidence-dir .../x2-render-evidence-verify --confirmation <the same test file citing
  607f2798…> --ledger <the gate's own edited-ledger.json>` — the gate's own hand-edited bypass attempt (a
  ledger row with `607f2798…`'s exact sha256+method but `normalizedUrl: "x"` / `url: "x"`, gate finding 2b)
  — **still refuses to run**, same `NON-RECORDED capture` error as above. Gate finding 2b's normalised-URL
  match (not just method+SHA) is what makes this specific hand-edited row fail to match, live-verified against
  the actual `edited-ledger.json` file, not just against the corrected ledger this pass built.

**Should-fix: VI's two cited quotes also appear verbatim in Run 1's direct-fetch RAW BYTES of the same page —
checked, not assumed, before writing this.** Run 1's direct fetch of the Trail Pass page (`679670f8…` above)
extracted only 80 bytes of *text* (just the `<title>` — the SPA's text-extraction problem, "VI — fetched
successfully… but genuinely has no confirmable content" in Run 1's own STATUS above) — but the page's RAW
HTML bytes are a different matter: they still carry the page's Inertia/Vue `data-page="…JSON…"` payload, which
embeds the same content the client renders. Grepping the stored raw file
(`raw/679670f8291d7afcab1b59f8b71bb4e9aa586da2429e2f3462326c4692d41f31.html` under Run 1's
`x2-evidence-final/` evidence dir) directly for both cited quotes:

- The roster/completionUnit quote ("Passholders can redeem their card to play one round at each of the
  following participating Vancouver Island golf courses through the 2026 golf season. Courses include:
  Arbutus Ridge * Bear Mountain – Valley Course * Campbell River * Cowichan * Crown Isle * Fairwinds *
  Highland Pacific * Morningstar * Olympic View * Storey Creek") — **found, 1 verbatim match.**
- The season quote ("This pass can be redeemed for play between Jan 1 and December 31, 2026.") — **found, 1
  verbatim match.**
- (For contrast: the SAME roster quote does NOT appear in the homepage's raw bytes, `efe829bc…` — 0 matches —
  confirming this isn't a false positive from grepping too broad a haystack; the quote is specifically on the
  Trail Pass page, in both the render and the Run 1 direct-fetch raw bytes of that same page.)

**This is genuine first-party corroboration, independent of the render route**: the same two facts, on the
same page, are present verbatim in bytes this tool captured via a DIFFERENT method (a direct `fetch()`, no
Chromium involved). **Correction (should-fix, Addendum J re-gate, second round): this was previously stated as
"over four hours earlier than the render" — that arithmetic was wrong.** The direct capture grepped above
(`679670f8…`, from `x2-evidence-final/`) was taken at 2026-09-24T18:18:58.520Z; the render was taken at
2026-09-24T18:57:26.575Z — the gap between those two specific timestamps is **about 38 minutes**, not four
hours. (The TRUE first direct capture of this same content, per the corrected table above, was even earlier —
2026-09-24T18:13:40.067Z, ~44 minutes before the render — but the corroboration grep itself was run against
the `x2-evidence-final/` capture specifically, so the 38-minute figure is what actually describes the bytes
grepped, and is the one corrected here.) The exact gap does not change what the check found: the same two
facts were captured, independently, by two different fetch methods, well under an hour apart. It does not
change which evidenceSha `x2-verdict` cites — the render capture remains the recorded one, and its own
extracted *text* is what the tool's automated quote-check reads — but it is recorded here as an honest,
checked fact: VI's roster/completionUnit/season facts were not only ever seen once, under one fetch method.

### How TN, VI and RTJ evidence combine into the one manifest `x2-verdict` reads

`x2-verdict` reads exactly ONE evidence directory's `manifest.json` per run (`--evidence-dir`). Each trail's
evidence can be a mix of methods within that same manifest — `x2-fetch`/`x2-fetch --render`'s own entries and
any `x2-ingest`-merged owner-saved entries all live in `manifest.trails[trail][]` together, exactly as
`x2-ingest`'s merge-into-an-existing-manifest behavior already does (see `tools/p0/README.md` §4). Concretely,
for the final X2 verdict once TN is in:

1. `x2-fetch --render --config <VI-only config> --out-dir <one evidence dir>` — already run (the recorded VI
   captures).
2. `x2-ingest --trail TN --file <owner-saved.html> --url <stated URL> --date <stated date> --out-dir <the SAME
   evidence dir>`, once per TN page Matt saves — merges TN's `owner-saved` entries into that same
   `manifest.json`, alongside VI's `rendered` entries.
3. RTJ's evidence (Run 1's `direct` captures, already confirmed roster-only) would need its OWN prior
   `x2-fetch` run's evidence dir merged the same way if a single combined directory is wanted for one
   `x2-verdict` invocation — `x2-ingest`'s merge-into-existing-manifest behavior is generic (it doesn't care
   whether the existing manifest came from `x2-fetch` or `x2-fetch --render`), so pointing any of these tools'
   `--out-dir` at the same directory is what combines them; nothing about the merge is TN-specific.
4. One `x2-verdict --evidence-dir <that combined dir> --confirmation <one file covering TN, VI and RTJ>
   --ledger docs/p0/x2-recorded-ledger.json` then produces the single, final X2 overall verdict — TN's facts
   backed by `owner-saved` evidence (plus corroboration per the Addendum J correction), VI's by `rendered`
   evidence, RTJ's roster by `direct` evidence (with `completionUnit`/`season` left unconfirmed, per both runs
   above). **`--ledger` is REQUIRED as of the Addendum J re-gate, second round (gate finding 2c)** — the
   per-directory default was removed; the canonical ledger for TN/VI/RTJ's own captures is
   [`docs/p0/x2-recorded-ledger.json`](x2-recorded-ledger.json) (gate finding 2d).

   **Third re-gate: the trust root moved off the caller's own paths and local git refs, onto this toolkit's
   own checkout plus GitHub itself.** A prior round's ledger/acceptance checks resolved "the canonical path"
   from wherever `--ledger`/`--x2-log` happened to point, and checked reachability against the LOCAL,
   editable `origin/main` — a scratch `git init` repo with its own forged `ACCEPT` row and a bare
   `git update-ref refs/remotes/origin/main HEAD` satisfied both checks for itself, no review or GitHub
   visibility required. Fixed: `--ledger`/`--x2-log` must now resolve (`realpath`) to EXACTLY this toolkit's
   own `docs/p0/x2-recorded-ledger.json`/`docs/p0/X2.md` — pinned via this module's own `import.meta.url`,
   never derived from the path a caller supplies — or the result is UNOFFICIAL (for the ledger) / the
   acceptance simply isn't trusted at all (for `ACCEPT` rows); and every run fetches `main` FRESH from the
   hard-coded canonical URL, `https://github.com/mcorbett51090/GolfRaven`, into a ref this tool owns and
   always deletes-then-refetches (`git fetch --no-tags <url> +refs/heads/main:refs/x2-verdict/verified-main`)
   — the ledger's `git hash-object` is compared against THAT freshly-fetched ref, and an `ACCEPT` row's `git
   blame`d commit must be reachable from it, never from the editable local `origin`. A network failure or a
   fetch error gives UNOFFICIAL, never OFFICIAL — it is never silently treated as "assume this checks out."
   Until the ledger is committed AND actually PUSHED to `origin/main` on GitHub (not merely committed
   locally), the run needs `--allow-dirty-ledger` (its output reads UNOFFICIAL until then — or, if the ledger
   is simply not yet on GitHub's `main` at all, `x2-verdict` marks it UNOFFICIAL on its own, without needing
   that flag, and says why). A TN owner-saved fact's corroboration, if any, is either `x2-corroborate-
   wayback`'s own output (a real web.archive.org fetch, registered in the ledger under method `wayback`,
   never hand-typed text — gate finding 3) or Matt's dated acceptance.

   **Matt's acceptance requires a structured row, not a free-text id (gate finding 2, re-gate), and only a
   VISIBLE one counts (third re-gate).** The row must read EXACTLY `ACCEPT <trail> <fact> <full
   evidenceSha256> <YYYY-MM-DD> Matt`, added by Matt himself to this file's own `## Log` section below
   (`<fact>` is `completionUnit`, `season`, or `roster:<name>`, matching the roster entry's own name
   verbatim) — and a row sitting inside a fenced code block, an HTML comment, or an indented code block does
   NOT count (a gate review found both a comment-hidden and a fence-hidden forged row silently accepted
   before this existed). Should-fix: the acceptance date must be on/after the evidence's own
   `ownerSavedDate`, and no more than 1 day after the date of the commit that introduced the row. `x2-verdict`
   finds that exact, visible row, then uses `git blame` to find the commit that introduced it and requires
   that commit reachable from the freshly-fetched `refs/x2-verdict/verified-main` — printing the commit hash,
   author, date and `%G?` signature status in its output, so a human can audit the row's provenance.
   **Honest limit, corrected at the third re-gate:** every agent working in this environment acts with Matt's
   own GitHub credentials, and even a commit made through the API is GitHub-signed — so neither `git blame`'s
   author field nor the `%G?` signature status proves a commit is Matt's rather than an agent's; that was
   never a safe claim, and this file no longer makes it. What the fix genuinely buys is narrower and still
   real: **a forgery now has to land in GitHub's own shared, public history for `main`** (a real push a
   reviewer or Matt can see, not a same-named local ref anyone with write access to a checkout can fabricate
   with one command) rather than in a disposable local repo nobody else ever sees. The control that actually
   matters remains procedural, not technical: **agents must never write an `ACCEPT` row themselves; only
   Matt adds one, by hand.**

   **Scope statement (seventh re-gate, round 8 — what the fix defends against, and what it does not):**
   the fourth re-gate's fix still ran every GitHub-verification git command (`fetch`, `git blame`,
   `merge-base --is-ancestor`, `rev-parse`) INSIDE the toolkit's own checkout, with the caller's
   `process.env` inherited wholesale — so any local git-state manipulation available to a caller in that
   same checkout or that same process's environment silently compromised the "verified against GitHub"
   read. The fifth re-gate (round 6) closed that: every such command runs inside a freshly created,
   disposable, BARE repository (removed on exit) under an explicitly constructed, minimal environment —
   never the caller's own `process.env` — with `GIT_NO_REPLACE_OBJECTS=1`, `GIT_CONFIG_NOSYSTEM=1`,
   `GIT_CONFIG_GLOBAL=/dev/null`, `HOME`/`GIT_DIR` both pinned to that disposable repo, and
   `-c core.hooksPath=/dev/null -c protocol.file.allow=never -c protocol.ext.allow=never` on every
   invocation. The sixth re-gate (round 7) found that fix still trusted the VERDICT PROCESS'S OWN runtime
   unconditionally — a fake `git` shim on PATH, or code injected into the process itself — and added: `git`
   resolved to a verified ABSOLUTE path (`/usr/bin/git`, falling back to `/bin/git`), refused unless owned
   by root and not group-/world-writable; every child git process's own `PATH` fixed to `/usr/bin:/bin`,
   never the caller's; and `detectRuntimeTamper`, which refuses (UNOFFICIAL) when the verdict process's OWN
   original environment shows `LD_PRELOAD`/`LD_LIBRARY_PATH`/`GIT_EXEC_PATH`/`DYLD_*`/a `NODE_OPTIONS`
   loader flag/non-empty `execArgv`.

   **The seventh re-gate (round 8) found that round 7's own framing of `detectRuntimeTamper` was itself
   false.** Round 7 listed `NODE_OPTIONS` loader flags and non-empty `execArgv` under "defends against."
   They are NOT defended against, and cannot be by any check added to this module. The round-8 gate
   fixture proved it directly: `evil.cjs` was extended with `delete process.env.NODE_OPTIONS;
   process.execArgv.length = 0;` at its very top, run BEFORE the rest of its payload (which patches
   `child_process.execFile`) and therefore BEFORE `detectRuntimeTamper` ever executes — `NODE_OPTIONS`
   preload code runs before a single line of this module's own code, full stop. Live-confirmed this round:
   with the self-erasing preload, `githubVerification.ok: true` and TN confirmed — the exact same OFFICIAL
   result as with no attack at all. This is not a bug in the check; it is structural. Any in-process
   detector reads `process.env`/`process.execArgv` no earlier than the attacker's own preload code did, so
   a preload that also erases its own tracks always wins the race — there is no ordering this module could
   choose that runs before `NODE_OPTIONS` preload code, because `NODE_OPTIONS` preload code is defined to
   run first. `detectRuntimeTamper` is downgraded, honestly, to **a tripwire for NAIVE, NON-SELF-HIDING
   injection only** — it still catches an attempt that patches `execFile` but doesn't bother erasing
   `NODE_OPTIONS`/`execArgv` afterward (confirmed this round: the SAME `evil.cjs` payload, without the
   self-erasing line, is still refused, by the round-8 `NODE_OPTIONS` allow-list below) — and it is kept in
   place for exactly that: defense in depth against a naive attempt, not a security boundary against a
   deliberate one.

   Round 8 also closed two gaps in the naive-injection tripwire itself, cheaply, while it's honestly only a
   tripwire: (1) `LD_PRELOAD`/`LD_LIBRARY_PATH` were a fixed two-name list that missed `LD_AUDIT` (the
   round-8 gate's own probe used it to load a native shared object into the process — live-confirmed this
   round: the library's constructor ran and left its own marker file) — `LD_*` is now checked as a PREFIX,
   matching `DYLD_*`'s own existing treatment. (2) `NODE_OPTIONS` moved from a DENYLIST of known-dangerous
   flags (which round 7 itself proved incomplete — it never named `--inspect*`, `--env-file`,
   `--conditions`, `--openssl-config`/`--use-openssl-ca`, `--preserve-symlinks*`, or any `--experimental-*`
   flag) to an ALLOW-LIST of known-benign ones: `--max-*` (any memory-limit flag), `--stack-size`,
   `--unhandled-rejections=<any value>`, `--no-warnings`, `--enable-source-maps`, `--trace-warnings`. Every
   other flag refuses, including every one just named. This environment's own ordinary shell's
   `--max-old-space-size=8192` NODE_OPTIONS still passes, confirmed live this round.

   **After this fix, the verdict defends against, specifically:** repo/global/system/XDG/HOME git config
   and the `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_n`/`GIT_CONFIG_VALUE_n` environment-variable channel
   redirecting the fetch (`insteadOf`); `GIT_DIR`/`GIT_WORK_TREE` set in the caller's own environment;
   `git replace`, `.git/info/grafts`, and `.git/hooks` (all local git-state forgery classes); a
   shallow-fetch boundary commit misrepresenting reachability; `GIT_PROXY_COMMAND` and any other
   non-allow-listed `GIT_*`/proxy variable (dropped by the scrubbed child env, never merely "unchecked");
   and a fake `git` binary reachable only via PATH manipulation (the absolute-path resolution plus
   root-ownership check). **It does NOT defend against — moved OUT of the list above, round 8's own
   correction of round 7's overclaim:** `NODE_OPTIONS` loader flags, `process.execArgv`, `LD_PRELOAD`/
   `LD_LIBRARY_PATH`/any other `LD_*` variable/`GIT_EXEC_PATH`/any `DYLD_*` variable — `detectRuntimeTamper`
   is a tripwire for a NAIVE attempt using these, never a defense against a deliberate one, for the
   structural reason explained above. **Residual #1, extended (round 8):** an agent that injects ANY code
   into the verdict process — including through the very variables named above, since injected code runs
   FIRST and can erase the indicators `detectRuntimeTamper` looks for, exactly as round 8's own gate
   fixture demonstrated — or that edits the toolkit's own source outright (the check IS the source, at that
   point). Two more environment variables belong in this same residual, named explicitly rather than left
   implicit: `NODE_EXTRA_CA_CERTS` is on this module's OWN proxy/CA allow-list (genuinely needed for this
   environment's TLS-intercepting proxy to work at all) — an agent that controls it controls a trust anchor
   for the "verified against GitHub" fetch, a TLS-layer residual next to the existing "an agent that
   controls the TLS-intercepting proxy or CA" one below; `NODE_PATH` is a Node-specific module-resolution
   search path — if set, a bare `require(...)` anywhere in this process's own dependency tree could resolve
   to an attacker-planted module, the same injection class as `NODE_OPTIONS`/`--require`, just via a
   different mechanism this module does not and cannot check for from inside itself. Other residuals,
   unchanged: an agent with enough privilege to defeat the git-binary check itself (e.g. root, which can
   chown/chmod `/usr/bin/git`, or a TOCTOU race against the check-then-use window); a CONCURRENT, SAME-USER
   process racing to write into the disposable repo's directory between its creation and use (this
   environment's own threat model already assumes an agent runs with the SAME OS user as this process, so
   filesystem permissions alone can never separate "the verdict process" from "an agent's other process"
   here — a structural limit of a shared-credential, shared-user session, not a bug in this check); an
   agent that controls the TLS-intercepting proxy or CA this environment's outbound HTTPS goes through
   (this environment's own — see the repo root's `README.md` / environment notes); or an agent using Matt's
   own GitHub credentials to push a forgery straight to `main` (the shared-credential limit already stated
   above — this fix narrows WHERE a forgery has to live, it does not identify WHO pushed it). All of these
   residuals are procedural controls, not technical ones: only Matt writes `ACCEPT` rows, and Matt reviews
   `main`'s own history on GitHub. The value of everything above is narrower than round 7 claimed: it
   raises the bar against a NAIVE forgery attempt (a stray fake git shim, an unhidden `--require`, a bare
   local ref) and closes every LOCAL GIT-STATE forgery class outright, but a deliberate, self-hiding
   process-level compromise of the verdict process itself is not, and cannot be, defended against from
   inside that same process — that is an honest, permanent limit of this architecture, not a todo for the
   next re-gate. This is the accepted residual for the next re-gate.

   **web.archive.org is confirmed unreachable from this session's environment** (direct `fetch`/`curl`, with
   and without `NODE_USE_ENV_PROXY=1`, both fail — a connect timeout/connection reset through the agent proxy,
   not a code bug; re-confirmed across three re-gate rounds now, same result each time — by contrast,
   `https://github.com/mcorbett51090/GolfRaven` IS reachable directly from this same environment, confirmed
   by both `git ls-remote` and this round's own `fetchVerifiedMainRef` runs actually succeeding). **No
   Wayback corroboration can currently be produced from THIS environment, full stop** — `x2-corroborate-
   wayback` is built and tested entirely against an injected fetcher, never the real network here.
   Concretely, this means a TN owner-saved fact that needs corroborating has exactly two live paths: **(a)**
   Matt adds a structured `ACCEPT` row for it himself, as above, or **(b)** someone runs `x2-corroborate-
   wayback` from a machine that CAN reach `web.archive.org` (its own network, not this session's) and merges
   the resulting corroboration record + ledger entry in. There is no third path from here.

This run did not combine them into one directory (VI's evidence and RTJ's Run 1 evidence remain in their own
separate `/tmp/golfraven-p0/...` locations) because TN — the piece that would make a combined run meaningful
— is not ready yet; the mechanism above is documented so the actual combining run is a known, mechanical step
once it is.

### X2 overall verdict — status after this partial run

**Pending TN owner-saved pages.** VI is now confirmed (this run, quotes corrected and re-verified against the
new first-party check above). RTJ remains unconfirmed (Run 1: roster confirmed; `completionUnit`/`season`
genuinely absent from the site — now checked under a SECOND fetch method, rendering, across four pages, and
still absent). TN has not been re-attempted — Matt's owner-saved pages (Addendum J(a)(ii)) are still needed
before `x2-ingest` can run for it. **X2's overall KILL/PASS is not re-derived from 1 of 3 (VI) — it stays
"pending TN owner-saved pages"** until TN's status is known, per this task's explicit instruction not to
re-derive KILL or PASS until TN is in.
