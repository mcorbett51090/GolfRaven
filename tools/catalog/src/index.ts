/**
 * `@golfraven/catalog-tools` — `verify-catalog` and `verify-contract`
 * (build plan §10 P1, §3.5, §15). Each is also runnable directly as a CLI
 * from `dist/`; see `tools/catalog/README.md`.
 */
export * from "./bundle.js";
export * from "./verify-catalog.js";
export * from "./verify-contract.js";
