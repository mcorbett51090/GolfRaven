{
  "name": "@golfraven/catalog-tools",
  "private": true,
  "version": "0.0.0",
  "description": "GolfRaven catalog governance CLIs: verify-catalog (build plan §10 P1 AT(1)/(5)/(7)/(8)) and verify-contract (§3.5, §15). Kept out of packages/catalog because that package \"Never does: Hold data or fetch\" (§3.1 row A) and these tools read data/ and write contract/.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "verify-catalog": "node dist/verify-catalog.js",
    "verify-contract": "node dist/verify-contract.js"
  },
  "dependencies": {
    "@golfraven/catalog": "workspace:*",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
