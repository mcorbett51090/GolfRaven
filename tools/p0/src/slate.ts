/** The pilot slate's three trails (build plan §10 P0; decision 0001), shared
 * by `x2-verdict.ts` and `x4-verify.ts` so the set is defined exactly once. */
export const SLATE_TRAILS = ["TN", "VI", "RTJ"] as const;
